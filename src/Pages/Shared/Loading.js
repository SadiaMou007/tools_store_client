import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center mt-12 h-screen">
      <button className="btn loading text-primary bg-white border-0">
        Loading...
      </button>
    </div>
  );
};

export default Loading;
